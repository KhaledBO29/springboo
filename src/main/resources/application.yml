spring:
  application:
    name: vehiculos-api
  data:
    mongodb:
      host: localhost
      port: 27017
      database: vehiculos_db

server:
  port: 8080

logging:
  level:
    com.example.vehiculos: DEBUG
    org.springframework.data.mongodb: DEBUG
